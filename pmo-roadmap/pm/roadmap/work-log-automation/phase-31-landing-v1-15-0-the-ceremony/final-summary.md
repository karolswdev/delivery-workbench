# Phase 31 - Landing v1.15.0 - the ceremony — Final summary

**Closed:** 2026-07-29. 2/2 stories done.

## Outcome vs exit criteria

- **The ceremony on film — met.** `demos/rendered/full-pipeline.mp4`
  (5:21, h264 720p) shows the complete pipeline live and unmocked:
  empty directory, `dw init`, the intake answers and the scope
  conversation's inert proposal, `dw program scaffold`, the Workbench
  browser reviews (adoption + Program Studio bundle), one setup
  lease with its token on screen, the gated adopt commit, the
  start-story lease, the explicit capability/budget grant,
  `dw program start --approve`, live supervised delivery — claude
  (opus) implementing in an isolated worktree, both closed checks,
  live codex (gpt-5.4) certifying — Mission Control tracking the run,
  certified handoff, operator integration (`git apply --index`),
  `node --test` 35/35, evidence capture, the guarded finish, the
  phase close, the gated ship commit, `dw verify --all`, and two
  live WebSocket clients playing the delivered game on one recording
  clock. Fully regenerable from checked-in tapes, recorders, intake
  assets, and scripts under `demos/full-pipeline/` (WLA-31-01).
- **v1.15.0 live — met at tag time.** Phases 22-31 land as one
  release: version surfaces in lockstep, the CHANGELOG's Unreleased
  accumulation rewritten as one phase-linked section, core suite
  692/692, package smoke green (four packaged exams replayed on the
  installed wheel), brew formula smoke green (WLA-31-02).
  Publication (GitHub Release, PyPI via the `pipit` trusted
  publisher, formula stamp, tap mirror, cold-install confirmation)
  proceeds immediately after the tag under the standing
  authorization, renewed by the owner 2026-07-28.

## What shipped beyond the criteria

- The ceremony behaved like an exam and was allowed to: it flushed
  out and fixed the Python-shaped `diff-scope` scaffold default that
  failed every Node/web delivery (now covering mainstream layouts,
  goldens regenerated), and two stale release-only assertions in
  `package-smoke.sh` (the pre-scaffold program verb list; the
  pre-front-door "empty consumer = attention" expectation).
- The demo kit itself: six VHS tapes with fail-fast Wait markers,
  three Playwright recorders (review, mission control, single-clock
  two-client gameplay), prepare/compose scripts, and a README that
  records the recording lessons (viewport-bounded output, VHS string
  quoting, ESM resolution, rider network requirements).

## Deliberately deferred

- Hosting or embedding the film anywhere beyond the repository
  (owner's call; GitHub cannot inline mp4 from a repo README).
- Rider-driven scope conversation on camera (the film shows the
  drafted proposal and names /dw-scope; a live TUI segment was
  judged too flaky to gate the release on).
