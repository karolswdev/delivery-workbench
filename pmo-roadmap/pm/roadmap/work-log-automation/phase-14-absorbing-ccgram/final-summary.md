# Phase 14 Final Summary

**Status:** complete — 7/7, closed 2026-07-04, opened same day.
**Release:** v1.11.0 carries the phase out the door.

The Absorption: alexei-led/ccgram (v4.3.5, MIT, lineage six-ddc)
studied in full and re-interpreted under the workbench's consent
spine. Twenty ideas mapped absorb / transmute / refuse (WLA-14-01,
`docs/absorption-ccgram.md`); the Telegram interface came out
transformed:

- **Instant push** (WLA-14-02): a dw-native hook seam
  (`dw hook install|status|emit`) appends whitelisted events; the
  interface drains by byte offset, so a blocked agent reaches the
  phone in ~1 s. Live on this desk.
- **The message layer grew up** (WLA-14-03): entity formatting
  (nothing left to escape), a per-chat FIFO queue with
  merge/coalesce/flood-patience, edit-in-place cards, and the
  first pure decision kernel.
- **Topics are projects** (WLA-14-04): one forum topic = one rails
  repo, commands scoped, questions routed home — and the owner's
  decision made real: consent gates entry, `/steer` binds a
  session, and then conversation FLOWS with no per-message tap.
  Flat mode is the degenerate one-room forum, so Phase 13 held
  untouched.
- **The driver's manners** (WLA-14-05): the settle-then-Enter TUI
  craft, harnesses as capability data, `/live` with content-hash
  gating, the `/toolbar`, capability-aware recovery. The
  pane-ownership check is one shared method now.
- **Seven locks** (WLA-14-06): `/send` behind containment, hidden,
  secret patterns, size, gitignore, gitleaks, and the workbench's
  own state-file lock — clean files send freely, hazards refuse by
  name.
- **The pocket desk, whole** (WLA-14-07): every organ composed in
  one end-to-end exam, plus architecture fitness tests that guard
  the consent floor (transport is a pure leaf, `send-keys` lives
  only in the driver) — self-verifying and CI-wired.

**Attribution:** ccgram is MIT (alexei-led, lineage six-ddc); this
phase re-interpreted its ideas and, where shapes were ported
(the send-security pipeline, the hook-installer discipline, the
byte-offset reader), carried the notice. Their thesis — sit on the
multiplexer, never wrap the SDK — we kept whole; their user-ID
allowlist auth we refused, pairing stands.

**The honesty bar held.** Refusals stayed in: the global-dw hook
trap caught pre-fire, the macOS symlink routing bug, the gitleaks
lock abstaining below Python 3.11, and the owed manual phone leg
named rather than faked (the same split Phase 13 carried). Journal
entries 16–22 are the narrative.

Owed beyond the phase: the manual phone-leg screenshots (the bot
serves now); everything else a fixture can prove is proven.
