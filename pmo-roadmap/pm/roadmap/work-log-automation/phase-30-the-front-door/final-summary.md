# Phase 30 Final Summary

**Status:** complete — 10/10 stories done.
**Date:** 2026-07-28.

## Outcome versus exit criteria

Every exit criterion closed with captured evidence. The phase promise —
**empty directory to granted program: one command, one conversation,
three approvals** — is machinery now, and the exit exam proved it end
to end: `dw init` boots an empty directory to healthy rails; the
`/dw-scope` conversation drafts one inert provenance-traced proposal;
`dw program scaffold` compiles typed answers plus that draft into a
complete governed bundle (six capabilities against the hand-written
Phase 29 bundle's nine, budgets as formulas, validation and pure
simulation as internal post-conditions); the workbench renders both
the adoption review and the Studio bundle review without minting or
accepting any token; one `setup-sha256:` lease lands the whole setup
atomically; the existing grant surface issues finite runtime authority;
and the live claude × codex cell delivered URL Sentinel's first story
to a certified handoff with no commit authority, leaving one bounded
lesson that the next grounded packet retrieves. Certification stayed a
hand-checked contract; commit stayed a human command; `dw verify` runs
green over the exam repository's whole history; the cold-install
repetition passed in a second neutral directory.

## What shipped

The setup-proposal contract (`delivery-workbench-setup-proposal@1`),
`dw init`, the `/dw-scope` rider skill, the atomic `dw setup
preview`/`apply` lease surface (retiring public `dw adopt --apply`),
the adoption review and the Program Studio bundle review inside the
existing workbench routes, whole-bundle program validation (rubric
facts, budgets versus team shape, compiler/conductor parity for node
types AND builtin check names, roster diagnostics), `dw program
scaffold` with `--proposal` base-draft composition, and
`knowledge:lesson-writeback` for no-commit grants — closing Phase 29's
named obligation. Suite grew 604 → ~700 across the phase.

## What the exam itself fixed

The exit exam ran five attempts, 8 grants total against Phase 29's
13-grant baseline; the successful attempt used one. Attempts 1–4 each
forced a shipped fix: conductable check vocabulary with a validate-time
builtin-parity diagnostic; pass-verdicts routing to the terminal
instead of the repair seat; a write-mode materialization channel for
declared non-diff outputs; deterministic tolerance for prose before a
JSON response document; and red rubric aggregates normalized into the
closed workflow route vocabulary. Attempt 4's final grant blocked
honestly on a real introduced-test-failure — the gates gating on live
bad code. Nine rider commits carry these fixes alongside the ten
story commits.

## Deliberately deferred

Guarded stranded-claim and stale-baseline-workspace recovery (bit
twice this phase; a runtime-hardening story next phase); visual
authoring of councils, rubrics, and nested workflows (generated
defaults plus JSON remain the expert path); hint-syntax
self-documentation before failure (bit again this exam); and the
release itself — the landing packet (exam transcript, artifact chain,
grant ledger, cold-install proof) is assembled in the exam evidence
for the owner's decision, and the recorded settling-period decision
stands until Karol explicitly supersedes it.
