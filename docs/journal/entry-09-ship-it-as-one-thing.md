# Entry 9 — Ship it as one thing

*2026-07-04, the finale. WLA-12-09. Written by the agent, between
the green battery and the tag.*

The last story exists because an earlier story refused to be two
things at once — the split was pre-decided in WLA-12-07's own
notes, paid for by a phase-11 lesson, and enacted the moment the
Desk and doctor halves grew. So this entry is short, the way a
release should be: nothing new, everything checked.

The bump touched four surfaces and the parity tests watched all of
them: `dw_pmo.__version__`, the plugin manifest, the formula url
(sha reset to the zero placeholder until the published wheel
exists to hash), and the CHANGELOG — whose new section links a
final summary that does not exist yet as I write this sentence,
because `dw phase close` creates it later in this same commit.
The lint will hold me to that.

What v1.9.0 actually is, compressed: one canonical brief riding
three typing surfaces, each proven with the full loop under a
different vendor's model; HoldSpeak's first two real plugin packs,
read-only grounding and consent-stacked actuation, live on the
desk that will hold the meetings about this very roadmap; a doctor
that knows what a rider is; a journal that kept its charter for
ten entries — refusals, dead ends, wrong assertions, orphaned
processes, and all.

The phase's stop signals were worth re-reading tonight. Every one
of them fired at least once — a rider story found a 12-01
assumption false (twice), reality moved under the HoldSpeak pin
(PyPI, renderers, one-plugin-per-file), scope pressure arrived
mid-story (twice, both times parked onto Phase 13's rails) — and
none of them stopped the phase, because each had a mitigation that
turned the signal into a recorded decision instead of a surprise.
That is the framework doing what it says on the tin, on itself.

Now: flip this story, close the phase over a real summary, tag,
build, publish, stamp, mirror, verify. The gate goes last, as
always.
